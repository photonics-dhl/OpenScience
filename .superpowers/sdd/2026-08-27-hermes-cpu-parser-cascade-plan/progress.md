# SDD ledger — plan: docs/plans/2026-08-27-hermes-cpu-parser-cascade-plan.md

Planning base: `e50a560`
Operational production baseline: application/release `e2c0eaf3b13a220a8bc2cd49b2c1dfe40a6fd61f`; rollback `8163f8b4218e529ee4be41bb9fc732ff6497931a`.

Task 1: in progress (implementation/spec review clean at `5bcef6f`; external evidence only). Current/Tesseract/LiteParse ECS evidence, isolation, cleanup and registry are complete. Docling lacks final model/image/corpus evidence; corrected PaddleOCR lacks completed image/model/preflight/scan evidence. Neither candidate is promoted.
Task 2: complete. V2 protocol (including canonical response-envelope closure), isolated transport, same-release V1 transition, concrete parser-service composition and production-image module packaging passed all local gates and independent review; ECS image start remains final-acceptance evidence.
Task 3: complete at `49cd909` + review fix `ac94ebc`. Deterministic Markdown/TeX/CSV/XLSX/DOCX/native-PDF `DocumentParser`, versioned virtual geometry, bounded `yauzl@2.10.0` XLSX handling, scan-time row/field/block caps, controlled PDF/DOCX expansion budgets, strict `A1:XFD1048576` references, deterministic IDs and exact provenance passed focused `62/62`, full worker `210/210`, build/typecheck/lint and runtime contract gates; no layout candidate was promoted.
Task 4: complete at `85ba051`. Provider-neutral V2 layout normalization and bounded fail-closed GROBID TEI enrichment passed focused `20/20`, worker/build/typecheck/lint and independent review; no concrete layout route was promoted. The single exact-SHA ECS GROBID attempt hit its 180s image-pull cutoff before digest/container/corpus execution, cleaned exact resources to 0 and remains `APPROVED_PILOT`; production Compose is unchanged.
Task 5: code complete at `622cc24`; independent review Ready with no findings. Local OCR/routing/resource gates and exact-SHA ECS full source build pass. Final packaged-image/runtime OCR and capped-load worker-responsiveness acceptance remain pending after the single bounded image build was terminated and exact candidate resources were verified clean; no Paddle promotion and production unchanged.
Task 6: fix round 1/5 (4 addressed, 0 open — post-local OCR quality routing, source identity/mutation isolation, strict aggregate bounds, locale-independent merge; commits `794beaa..b2702a1`).
Task 6: complete (commits `e77f1d4..b2702a1`, review clean). Candidate-only LLM OCR is capped at four pages, fail-closed behind trusted authorization/external eligibility, preserves deterministic/local evidence and returns `needs_review` on disabled/outage unresolved pages; MiniMax/Vision remains production-disabled.
Task 7: fix round 1/5 (2 addressed, 0 open — execution-time role/workspace/policy authorization and real scan/candidate startup self-test; commits `7873479..7fcf60a`).
Task 7: complete (commits `b2702a1..7fcf60a`, review clean). Artifact-backed `sdf.extract` invokes one canonical cascade, compatibility prompt text is derived from its source map, trusted eligibility is active-workspace/allowed-role/policy deny-default, and production Vision/candidate routes remain hard-disabled. Focused `64/64`, worker `290/290`, domain `433/433`, AI Gateway `48/48` passed; ECS packaged runtime remains Task 8 acceptance.
Task 8: BLOCKED after fix round 5/5 at candidate `6268be376b70378e78fb09ee0f129abfd83ccc33`. Exact-SHA GitHub Actions run `33173849289` / job `98857320148` and local code gates passed (focused `139/139`, worker `366/366`, composition `2/2`, API `68/68`, build/typecheck/lint/audits/docs/diff/credential gates), but these are not production acceptance. Final breaker review found two load-bearing defects: the parser image allowlist omits `native-pdf-contract.js` and `native-pdf-text-items.js`, and native PDF geometry double-flips PDF.js viewport Y coordinates. Required next work is a fresh reviewed fix with packaging-contract coverage plus asymmetric/rotated PDF geometry tests, then a new exact SHA and CI. Phase B ECS deployment/acceptance was not started; production remains unchanged.
Task 8 blocker recovery: complete at `0ac37fe` after explicit user resume. A fresh TDD cycle reproduced all three failures (two missing packaged modules, asymmetric Y mirror, rotated-page Y mirror), added production-shaped packaging-closure and hand-derived geometry regressions, and passed independent task review with no Critical/Important/Minor findings. Fresh controller gates: compiled `5/5`, Agent Worker `366/366`, typecheck/lint/docs-sync/dependency/duplicate/diff checks pass. Exact-SHA push/CI and ECS Phase B remain pending; production is unchanged.
Task 8 release-contract recovery: complete at `82e9986` (`65e934b..f2636be` implementation plus Linux harness stabilization). Archive source identity, runtime closure, acceptance report/final image binding, active rollback equality, single-process inherited-FD9 deployment, durable journal, expected-only CAS and lock-held rollback passed repeated adversarial architecture/security review with no findings. Exact Ubuntu CI run `33192991542` succeeded, including the five Linux-only flock/ownership/signal/journal gates. ECS Phase B remains pending and production is unchanged.

Preflight resolution: the plan's embedded rollback `f965966...` is stale. All production operations use the CURRENT tuple above and must refresh it from ECS immediately before deployment.
