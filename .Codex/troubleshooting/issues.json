{
  "issues": [
    {
      "id": "issue-001",
      "title": "Figma MCP enabled but OAuth authentication unavailable",
      "date": "2026-08-08",
      "status": "resolved",
      "tags": [
        "troubleshooting/mcp",
        "troubleshooting/auth"
      ],
      "description": "Codex listed figma-temp and figma-primary as enabled, but neither produced a ready MCP client and the UI reported authentication failure.",
      "root_cause": "A remote MCP entry being enabled does not perform OAuth login. Both servers were correctly configured but remained in the Not logged in state; the Codex App did not complete the explicit login flow.",
      "solution": "Use the official Codex CLI against the shared config: run `codex mcp login figma-temp` and `codex mcp login figma-primary`, completing each browser authorization with the intended Figma account.",
      "verification": "`codex mcp list` reports both Figma servers as enabled with Auth=OAuth. A fresh Codex App session is still required to expose their tools, and account separation must be checked with account-exclusive test files.",
      "files": [
        "C:/Users/Mac/.codex/config.toml",
        "docs/decisions/ADR-004-figma-account-ownership-and-migration.md"
      ],
      "prevention": "After adding an OAuth HTTP MCP, check `codex mcp list`; if it says Not logged in, run `codex mcp login <server-name>` before restarting the client. Never equate enabled with authenticated."
    },
    {
      "id": "issue-002",
      "title": "Two Figma MCP servers reused the same browser identity",
      "date": "2026-08-08",
      "status": "resolved",
      "tags": [
        "troubleshooting/mcp",
        "troubleshooting/auth"
      ],
      "description": "Both figma-temp and figma-primary authenticated successfully, but their whoami calls returned the same temporary-account identity.",
      "root_cause": "The second browser OAuth flow reused the Figma account already signed in to the default browser. Separate MCP server names isolate stored credentials, but they do not choose different browser identities automatically.",
      "solution": "Log out only figma-primary, switch the default browser to the long-term Figma account, then run `codex mcp login figma-primary` again. Leave figma-temp credentials unchanged.",
      "verification": "After restarting the Codex App, parallel whoami calls confirmed figma-temp and figma-primary have different emails and the expected account prefixes. The temporary account handle is Ran; the long-term account handle is zju.",
      "files": [
        "C:/Users/Mac/.codex/config.toml",
        "docs/decisions/ADR-004-figma-account-ownership-and-migration.md"
      ],
      "prevention": "Before authorizing a second server for the same OAuth endpoint, switch the browser to the intended account. Verify both identities with whoami in fresh clients before creating canonical assets."
    },
    {
      "id": "issue-003",
      "title": "Hermes acceptance video and production preview used different API conditions",
      "date": "2026-08-17",
      "status": "resolved",
      "tags": [
        "troubleshooting/env",
        "troubleshooting/browser"
      ],
      "description": "The release-gate WebM showed a healthy Dashboard and Hermes whole-character motion, while the user's 3194 page did not match it. The initially linked WebM also failed to load in the app.",
      "root_cause": "The release gate intercepted auth, research-object, and ingestion requests with successful fixtures, but 3194 had been started without `API_ORIGIN=http://127.0.0.1:3188`; its real server-side requests fell back to port 3001 and rendered `无法加载工作台 / 请求失败 500`. An already-open `next start` tab could additionally retain old JS/CSS after rebuild. The Markdown handoff referenced a local WebM filesystem path rather than an HTTP media URL.",
      "solution": "Start 3194 with the verified local preview API on 3188, then run and record an acceptance browser with zero API interceptions. After each production rebuild, open a fresh cache-busted Dashboard URL. Serve ignored visual evidence through a bounded localhost HTTP server instead of a local media path.",
      "verification": "The unmocked 3194 browser loads Ada Researcher and the real preview RO without the 500 error; Hermes reports idle/ready with reduced motion false, exercises six actions in 18 seconds, and moves across a 33x23px range. The final 35.8-second 1440x900 WebM uses zero API interceptions and has no console errors. Dashboard and video URLs both return HTTP 200, with the video served as `video/webm`.",
      "files": [
        "apps/web/app/globals.css",
        "apps/web/test/visual/out/hermes-companion/hermes-rich-motion-latest.webm"
      ],
      "prevention": "A release video must use the same API origin and data path as the handed-off preview. Before visual delivery, run one zero-interception browser probe that rejects Dashboard error copy, records the real body/state/renderer/action trajectory, and only then records video. Never ask for acceptance from a tab that survived a production rebuild; publish evidence over HTTP and verify status/content type."
    },
    {
      "id": "issue-004",
      "title": "Hermes motion video forced animation while the real Codex browser honored Windows reduced motion",
      "date": "2026-08-17",
      "status": "resolved",
      "tags": [
        "troubleshooting/env",
        "troubleshooting/browser",
        "troubleshooting/accessibility"
      ],
      "description": "The corrected Dashboard and video used the same API data, but the user still saw a static Hermes while the Playwright recording moved.",
      "root_cause": "Windows SystemParametersInfo reported ClientAreaAnimationsEnabled=false, so the Codex Chromium browser exposed prefers-reduced-motion: reduce. Hermes deliberately returned a still motion sample, skipped the WebGL renderer, and disabled CSS whole-character motion. The acceptance recorder explicitly forced reducedMotion: no-preference, so it did not represent the user's browser environment.",
      "solution": "Add one explicit Hermes motion preference resolver. The default continues to honor the system, while ?hermes-motion=full opts into full mesh, CSS whole-character actions, and pointer input. Scope reduced-motion CSS suppression to stages that have not explicitly selected full motion.",
      "verification": "With Playwright still emulating prefers-reduced-motion: reduce, the system URL remains fallback with zero travel, while ?hermes-motion=full reports rig ready/input true, performs eight actions in 20 seconds, moves the actor 25.39px with a 34.08px size delta, and enters engaged state on pointer input.",
      "files": [
        "apps/web/lib/hermes/motion-preference.ts",
        "apps/web/components/hermes/HermesWorkspaceStage.tsx",
        "apps/web/components/hermes/HermesVisualAdapter.tsx",
        "apps/web/components/hermes/HermesRiggedPortrait.tsx",
        "apps/web/app/globals.css",
        "apps/web/test/hermes-pet-motion.test.ts"
      ],
      "prevention": "Visual acceptance must run once with the user's real reduced-motion condition. Any recorder override must be disclosed and paired with an equivalent product preference; data attributes or test-only media emulation cannot substitute for the user's rendered result."
    },
    {
      "id": "issue-005",
      "title": "Real arXiv PDF failed before MiniMax extraction",
      "date": "2026-08-17",
      "status": "resolved",
      "tags": [
        "troubleshooting/ingestion",
        "troubleshooting/security",
        "troubleshooting/parser"
      ],
      "description": "The real 24,671,920-byte arXiv 2009.06045 PDF first failed malware policy and then entered parser review, despite being a legitimate paper that parsed correctly outside the production pipeline.",
      "root_cause": "The artifact scan applied the byte sequence ../ to every format instead of only ZIP archives, and the controlled parser limit was 20MB. After those fixes, direct string matching still rejected valid MiniMax evidence quotes because PDF extraction changed whitespace, punctuation and line-break hyphenation.",
      "solution": "Apply traversal detection only when ZIP magic is present while retaining global EICAR/PE checks; raise the bounded parser input to 50MB; match evidence by exact text, whitespace equivalence, then a unique NFKC alphanumeric canonical sequence, always persisting the original manuscript substring and locator.",
      "verification": "The real paper completed ECS upload, parsing, MiniMax extraction, review, confirm and version commit. Five supported fields have original-source character locators, while unsupported reproducibility remains explicitly missing. Focused artifact/worker tests preserve ZIP traversal rejection and the 50MB+1 boundary.",
      "files": [
        "packages/domain/src/artifact/scan.ts",
        "packages/domain/test/artifact/scan.test.ts",
        "apps/agent-worker/src/ingestion-parser.ts",
        "apps/agent-worker/src/extractor.ts"
      ],
      "prevention": "Production ingestion acceptance must include a real large PDF, exact source-locator verification and explicit missing fields. Generic byte heuristics must be gated by the file format they describe."
    },
    {
      "id": "issue-006",
      "title": "MiniMax workspace guide output repeatedly failed the strict result contract",
      "date": "2026-08-17",
      "status": "resolved",
      "tags": [
        "troubleshooting/ai",
        "troubleshooting/schema",
        "troubleshooting/production"
      ],
      "description": "A real Dashboard workspace.guide task called MiniMax-M3 successfully three times but ended failed with the structured-output retry limit.",
      "root_cause": "The prompt named fields without defining their exact types or allowed object keys and did not enumerate current authorized target IDs. MiniMax returned useful prose using title/description, a question array for needsMoreInformation, and open-task actions when no such target was available. Identical retries had no new constraint and repeated the mismatch.",
      "solution": "Keep the strict guard and server authorization unchanged. Supply membership-scoped SDF core fields to the model, state the exact JSON shape and forbidden keys, require a boolean needsMoreInformation, and enumerate the only allowed task/RO IDs for navigation.",
      "verification": "After a full ECS deployment, a real browser Dashboard submission succeeded in about 40 seconds, accurately identified the complete RO, the missing reproducibility evidence and duplicate diagnostic drafts, and returned only authorized RO/task navigation links.",
      "files": [
        "apps/agent-worker/src/workspace-guide.ts",
        "apps/agent-worker/test/workspace-guide.test.ts"
      ],
      "prevention": "Structured AI prompts must specify exact types, exact allowed keys and runtime allowlists; retries must not be treated as evidence that an underspecified contract will self-correct."
    },
    {
      "id": "issue-007",
      "title": "Deployed Hermes appeared static and did not guide the real creation workflow",
      "date": "2026-08-17",
      "status": "resolved",
      "tags": [
        "troubleshooting/browser",
        "troubleshooting/accessibility",
        "troubleshooting/product-flow"
      ],
      "description": "In real production use, Hermes did not visibly idle or respond, did not swim to key form locations, and did not explain what the user should enter at creation and SDF-editing milestones, despite motion-focused release videos passing.",
      "root_cause": "The user's Windows browser still resolves system reduced motion and the full-motion URL override is neither persisted nor exposed as a product control. Separately, only Dashboard registers the real assistant presentation and drawer; creation/edit routes render a neutral stage. Route guidance hard-codes only the first title/problem target, has no field-completion progression, and several displayed Explain/Draft/Check actions have no consumer on the current route.",
      "solution": "Make HermesWorkspaceStage the sole motion-preference owner, persist the explicit full/reduced choice, expose an accessible enable-motion control, and pass the resolved value into the adapter and rig. Keep a real route-owned assistant drawer on creation/edit pages, advance guide targets from title/import and selected SDF fields, and render only actions that have a consumer; Explain now shows field-specific guidance locally.",
      "verification": "TDD RED captured the missing persisted preference, unavailable motion control, inert create actions, and static ro-title/sdf-problem targets. GREEN evidence: focused motion 16/16, local and public production-browser creation/edit/motion matrices 6/6, Web 295/295, typecheck, canonical lint, 17-page build, and the 145.6-second Hermes release gate. The release gate measured 5,215 idle-breathing and 58,228 pointer-response changed pixels plus distinct head/torso/tail motion. ECS source hash matches release aa1c8af, restarted Web/API/Worker are healthy, and recent critical-error counts are zero.",
      "files": [
        "apps/web/components/hermes/HermesWorkspaceStage.tsx",
        "apps/web/components/hermes/HermesVisualAdapter.tsx",
        "apps/web/components/hermes/HermesRiggedPortrait.tsx",
        "apps/web/lib/hermes/motion-preference.ts",
        "apps/web/test/e2e/hermes-field-guide.spec.ts"
      ],
      "prevention": "A random-motion video is not a workflow acceptance test. Every Hermes release must be exercised in the actual user media preference and through the uninterrupted creation/editing milestones, with visible controls and route-owned action consumers."
    },
    {
      "id": "issue-008",
      "title": "Hermes visual-review keyboard menu raced asynchronous scene URL navigation",
      "date": "2026-08-24",
      "status": "resolved",
      "tags": [
        "troubleshooting/browser",
        "troubleshooting/accessibility",
        "troubleshooting/navigation",
        "troubleshooting/production"
      ],
      "description": "The first production acceptance run passed right-click, long press, fallback and reduced-motion cases but intermittently failed Shift+F10 after a rapid Editor to Review to Editor tab sequence.",
      "root_cause": "The visual fixture kept scene state locally while calling asynchronous Next router.replace for the query string. During a rapid Review to Editor return, the browser URL was still editor, so Next treated the second replace as a no-op while the first review RSC request remained in flight. Its eventual commit could race the newly opened context menu.",
      "solution": "Keep the fixture's local reducer as the interaction owner and update the same-route view query synchronously with window.history.replaceState. Add a browser contract requiring each Arrow-key scene change to update the query before the next input event.",
      "verification": "A deterministic RED assertion observed local state=review while the URL still said editor. After the single navigation-boundary change, the focused regression and full five-case local route suite passed. ECS hotfix b73a9dd deployed with no migration; public full acceptance passed 5/5 and the keyboard scenario passed three additional concurrent repeats.",
      "files": [
        "apps/web/components/visual/ResearchWorkbenchReview.tsx",
        "apps/web/test/e2e/research-workbench-review.spec.ts"
      ],
      "prevention": "For local-only same-route visual controls, do not mix immediate reducer state with an asynchronous server navigation solely to mirror a query parameter. Assert state and URL move atomically before testing subsequent keyboard input."
    },
    {
      "id": "issue-009",
      "title": "Isolated worktree deployment could not locate cloud sync configuration",
      "date": "2026-08-25",
      "status": "resolved",
      "tags": [
        "troubleshooting/deployment",
        "troubleshooting/worktree",
        "troubleshooting/configuration"
      ],
      "description": "A confirmed immutable deployment from the Hermes feature worktree stopped before upload because the worktree did not contain the ignored cloud sync configuration file.",
      "root_cause": "Deployment configuration is intentionally stored only at the main repository root, while the release command ran from an isolated worktree. The deploy script supports an explicit configuration root, but the first invocation did not set it.",
      "solution": "Keep the feature worktree as the release source and set XGS_CONFIG_ROOT to the main repository root for the deployment process. This lets the sync helper locate the existing ignored configuration without copying it into the worktree or exposing its contents.",
      "verification": "The first call failed before upload and left the active release, failure marker and candidate tree unchanged. The corrected call materialized the exact clean release, completed the server build and healthy container switch, and passed exact release plus public 5/5 Hermes acceptance.",
      "files": [
        "infra/scripts/deploy.sh",
        "infra/scripts/cloud-sync.mjs",
        "docs/runbooks/deployment.md"
      ],
      "prevention": "When deploying from an isolated worktree, always set XGS_CONFIG_ROOT to the main repository root before dry-run and confirmed release commands; never copy or commit the ignored deployment configuration into the worktree."
    },
    {
      "id": "issue-010",
      "title": "Ubuntu visual release gates sampled runner-dependent browser timing and mismatched DOM contracts",
      "date": "2026-08-26",
      "status": "resolved",
      "tags": [
        "troubleshooting/ci",
        "troubleshooting/browser",
        "troubleshooting/visual-regression"
      ],
      "description": "PR #4 passed build, typecheck, lint and unit tests on Ubuntu but exposed runner-dependent slow-pointer sampling, menu geometry, pixel-evidence duration, screenshot-clock speech ordering and a mismatched Hermes loading-state selector in the production visual gates.",
      "root_cause": "The browser case moved only 2px between host-timed waits and sampled one fixed snapshot. SwiftShader RAF cadence could therefore miss the 70ms response peak even though the deterministic interaction model met the slow-motion contract. Separately, Linux and Windows font metrics made the Hermes menu 1-1.5px taller than the design baseline, and the detached-menu cold-start case could position against pre-font geometry because it omitted document.fonts.ready. Ubuntu software rasterization could spend about 28 seconds decoding the 1920px evidence pair and exhaust the generic 60-second test budget during the final full-page evidence screenshot. The Hermes speech-order test also treated completion of two software screenshots as a product clock, so the valid 520ms feedback timer could fire before the second capture returned. Finally, the Hermes loading test searched for main[aria-busy=true], although DashboardShell deliberately forwards aria-busy to its data-os-surface=dashboard root; the selector could never observe the documented shell contract.",
      "solution": "Keep the exact 12px/1s >=.05 slow-response contract in the deterministic model test. In the browser, use a still-slow 12px stepped traverse and record the peak plus final pointer coordinate continuously on RAF instead of requiring every host-timed step to cross the threshold. Wait for fonts, two layout frames and menu animation settlement, and allow 2px cross-platform font-metric tolerance below the nominal 24px band while retaining independent zero-overlap and 48px upper-bound gates. Give only the three visible-optical cases a 120-second total evidence budget; do not relax any action-level timeout, perceptual threshold or geometry assertion. Record Hermes action and feedback DOM timestamps with a MutationObserver, retain the immediate no-speech and two-frame real-motion gates, and assert a >=450ms physical-reaction lead independently of screenshot duration. Assert the loading shell through its data-os-surface and aria-busy contract, verify its aria-live child, and wait for the mocked 503 response before inspecting the error DOM.",
      "verification": "The focused interaction model passed 18/18, the fresh production Web build passed, the optical cases passed 10/10, the detached Hermes cold-start case passed 10/10, the event-order case passed 5/5 (15 selections), and the complete production visual release gate passed 72/72. CI run 32959918188 trace recorded slowPeak 0.111 and the expected pointer coordinate before its final evidence-screenshot timeout; run 32961184540 then passed all three optical cases and failed only the obsolete post-screenshot speech absence check. After CI run 32962638531 exposed the impossible loading selector, the corrected loading/error case passed locally and the full production Hermes gate passed its 19 compatible-runtime and 8 product-interaction cases.",
      "files": [
        "apps/web/test/e2e/product-release.spec.ts",
        "apps/web/test/e2e/hermes-dashboard.spec.ts"
      ],
      "prevention": "Do not encode perceptual velocity through host-scheduled 2px browser events, block each input step on a transient peak, use screenshot completion as a product clock, assert portal geometry before fonts and animations settle, or guess a component's attribute owner from its visual hierarchy. Put exact time-domain thresholds in deterministic tests; browser gates should record observable RAF, API and DOM event order while preserving independent pixel-motion and physical-overlap checks. Budget software-rendered pixel evidence separately from behavior-level waits."
    }
  ],
  "last_updated": "2026-08-26"
}
