-- Application rollback retains the expanded constraint and narrative history.
-- Older profile/budget pairs are unchanged. Do not delete runs or narrow this
-- constraint when rolling back application code.
SELECT 1;
