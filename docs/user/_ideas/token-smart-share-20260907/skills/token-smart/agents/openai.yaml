interface:
  display_name: "Token Smart · 质量优先省量"
  short_description: "自动定向取证、收敛检查日志并执行实际分工，保留必要质量验证。"
  default_prompt: "使用 $token-smart 直接完成任务：定向取证、收敛日志、按需实际委派及网页分析，保留必要验收。"
policy:
  allow_implicit_invocation: true
