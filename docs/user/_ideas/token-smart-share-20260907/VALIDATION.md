# 验证范围
本包包含改进后的token-smart和可选auto-compact，未包含个人项目状态。
静态验证：skill frontmatter、隐式调用设置、相对引用、文件清单、脚本语法。
运行验证：计量逻辑的离线单元测试、run-check成功与失败返回；不调用额外模型。
行为案例见SCENARIOS.md，为逐条人工规则走查，不冒称新模型端到端实验。
历史实际网页连续交接/原生子模型调用证明可行路线，但新机器浏览器、登录、模型可用性仍须现场验证。
本轮子任务报告额度用尽，因此未开展新版路由收费对照，不能给新版净节省百分比。
压缩的字符/字节只是指令体积变化，不是任务token或账户费用节省率。

2026-09-07本机结果：入口+routing/handoff/metering四文件由11,529字符减至4,246字符（63.2%）；全部保留脚本node --check通过，metered-run离线13/13通过。run-check成功返回0，人工构造失败正确返回7且保留日志尾部。分享包未发现本机用户名、项目名、生产地址或个人Chat会话URL。未重新运行付费模型A/B。

2026-09-07 quality-main update：保留主模型；低档子任务按可验证性分配；客观失败触发重新分级。新增4个手工场景走查，不是运行模型的评测，不证明质量等同或净节省率。保留先前脚本验证；本次无脚本改动。
