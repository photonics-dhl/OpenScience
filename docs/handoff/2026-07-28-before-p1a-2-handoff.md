# Handoff — OpenScience XGS before P1A-2

- Current goal: Start Phase 1A task 2.2 — PostgreSQL + Redis + Storage Adapter data foundation for OpenScience MVP.
- Done: Phase 0 audit accepted; ADR-001 target architecture Accepted; ADR-002 tooling portability Accepted; task-master tasks.json repaired; P1A-1 pnpm Monorepo skeleton completed in worktree, reviewed, and copied clean into main checkout; `.agents/skills/docs-sync/SKILL.md` created; handoff rule fixed so handoffs live in `docs/handoff/`, and this handoff was moved back into the repo from a C: temp path.
- Evidence/commands: `node scripts/verify-workspace.mjs` → `WORKSPACE_STRUCTURE_OK` in main; worktree verification had `npx pnpm@9.15.0 install/build/typecheck/lint` green and `API_IMPORT_OK`; temp handoff cleanup command returned `TEMP_HANDOFF_REMOVED`; `docs/handoff/2026-07-28-before-p1a-2-handoff.md` exists in repo.
- Constraints: Do not read/print `.env`; no git commit/push unless user explicitly approves each mutation; AI routing main model is MiniMax-M3 and fallback must stay configurable; use `npx pnpm@9.15.0`, not global pnpm; project artifacts must live inside this repo, not on C:; routine docs sync is agent-active, formal handoff is written at phase boundary/long session/agent or machine switch/user request.
- Open risks: main checkout has many modified/untracked files; `.worktrees/p1a-1` remains with node_modules/dist/.next/src build residue (not copied); first future commit must recheck build residue and `.gitignore`; task-master MCP `set_task_status` still fails for subtasks, so subtask status changes may need JSON repair with backup; current session skill listing may not show `docs-sync` until reload, but the file is present and should be followed.
- Read first: `AGENTS.md` → `.agents/skills/docs-sync/SKILL.md` → `docs/OpenScience_Kimi_Development_Spec.md` → `docs/decisions/ADR-001-target-architecture.md` → `docs/decisions/ADR-002-agent-tooling-portability.md` → `docs/CODEBASE_AUDIT.md` → `docs/progress.md` → `project_index.md` → `docs/handoff/` → task-master task 2 → Memory entities `XGS-*`.
- Next action: P1A-2 design gate, then implement `infra/compose` dev stack, `packages/database` connection + migration runner, `packages/storage` Storage Adapter with MinIO dev backend. Keep SDF/editor (1B), collaboration (1C), Hermes/publish (1D), sandbox (1E), and Phase 2 features out of scope.
- Suggested skills next session: `docs-sync` for bookkeeping, `brainstorming` → `writing-plans` for P1A-2 design/plan gate, `database-migration` for migration rules, `architecture-guard` for package boundaries, `api-contract` if any API schema appears, `security-review` for storage/DB credential handling, `test-gate` before claiming completion.
