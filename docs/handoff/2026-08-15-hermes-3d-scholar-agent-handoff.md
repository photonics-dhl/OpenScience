# Handoff — 2026-08-15 Hermes 3D Scholar Agent

- Current goal: obtain the user's final visual acceptance for the original Hermes 3D scholar robot, then merge or deploy only under a separate explicit instruction.
- Done: original deterministic Blender asset and editable `apps/web/assets/hermes/Hermes.blend`; six real clips; 6-draw OGL runtime; SVG/reduced/failure fallback; desktop/mobile/lifecycle browser gate; production build; ADR/progress/index sync.
- Evidence: GLB `1,143,636 B` raw / `298,555 B` gzip, `19,024` triangles, six materials and six primitives; final-build SHA-256 `e8d121f629441af04478ce31dd26d1300e87ff2a41751a7bc671d535634c0003`; `HERMES_3D_GATE_OK`; Web `250/250`; Dashboard `130 kB` first-load JS. Review fixes include generation-owned teardown, ready→reduced→normal fallback continuity, one-shot failed recoil, strict clip/material/provenance parsing and real state light semantics. The final production-browser gate also injects a Hermes shader link failure and checks fallback plus exact WebGL2 acquire/release accounting across context loss, reduced-motion toggling and GLB/shader failure.
- Constraints: Blender 4.5.12 LTS Portable download/extraction/executable and reproducible build TEMP/TMP/profile stay on E:; no Wanko/Live2D binary; original SVG remains rollback; no `.env`, cloud, ECS or deployment change in this branch.
- Open risks: user has not yet given final in-product visual acceptance; two ignored local review residues (`Hermes.blend1` and the imagegen concept PNG) are not versioned and are not facts, but Windows command policy blocked their physical removal.
- Next action: show `apps/web/public/hermes/hermes-scholar-poster.webp` plus the runtime/turntable evidence to the user; after approval, decide separately whether to merge and deploy. This task intentionally uses one final atomic commit instead of preserving intermediate asset/runtime commits.
- Read first: `AGENTS.md` → `docs/OpenScience_Kimi_Development_Spec.md` → this handoff → `docs/specs/2026-08-15-hermes-3d-scholar-agent-design.md` → ADR-010 → `docs/progress.md` → `project_index.md`.
