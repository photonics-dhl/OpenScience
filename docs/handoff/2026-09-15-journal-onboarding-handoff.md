# CURRENT：期刊入驻与 AI 解读

## Goal / version tuple

- 用户要求：期刊申请元数据用英语、英文刊名必填/中文选填；移除专业审核服务并解释服务边界；提交后跳转成功页并显示真实编号。最新明确授权将其指定机构邮箱账号设为平台管理员；既有上线授权延续，保留其他任务改动。
- 最新纠正：补齐可发现、可进入的期刊审核入口。用户明确批准仅期刊审核取消独立后台密码、使用网站管理员账号登录；其他后台原认证保持。入口/页面/数据必须实际走通才交付。
- 当前交付树：项目目录下 `openscience-production-journals`，branch `codex/journals-production`；HEAD 以 Git 元数据为准。
- 原开发分支 `codex/journal-onboarding` / `261a38fa9fa5e97a099c59a082447ed87b43de05` 及 [PR #1](https://github.com/Nanqing96/openscience/pull/1) 保留；它基于旧版 49ff4fcd，禁止直接覆盖当前生产。
- 当前线上 release `3906b93d5fa3c911f3f2441ac6a6f28cf7dda5ec` / rollback `aee3f3184e6813223ca3f361922ec30b21b54fe3`。2026-09-16 canonical 发布成功并完成公网精确版本核对；后续操作重新定锚。历史生产版本保留。
- 集成树基于 6684e448，另保存 ba184541 的 9 文件、bdf0d3fa 的 4 文件线上增量；1988 个源文件逐一按发布清单核验哈希。原 SHA 未在 GitHub 可获取，快照提交明确记录来源，不冒称恢复原提交对象。
- `.env` 使用用户提供 `D:\00_codex\(1).env` 的 Git 忽略副本；项目 SSH 密钥已可用。没有输出凭据或将配置纳入提交。

## 当前部署进度

- 最新用户授权开发并上线退回修改闭环：清晰三种审核操作、拒绝二次确认、误拒更正为待修改、同编号继续修改/保存/重提。候选实现已完成；新更正接口要求管理员、rejected/未入驻、匹配修订号及必填原因，保留原审核意见和元数据，同事务审计。独立 high 复核 GO（仅新增 8 文件静态检查）；部署待完成，用户指定误拒申请将在部署后经真实页面更正，不修改其资料或批准入驻。
- 审核反馈修复已上线并推送：旧 Console 的页尾消息改为本行聚焦反馈，独立申请卡显示英文问题、提交中/防重、成功状态及只读刷新；补件说明只填表。新增差异 high GO；必要服务器构建、服务健康、版本切换与事务清除成功，无迁移；日志 `.work/journal-review-feedback-deploy.log`。
- 真实 Edge 已在指定旧申请点击“通过”：前端立即拦截四项中文资料，alert 获焦点且滚动到按钮附近，截图已观察；列表显示待审核。此点击在客户端返回，不重复发送审核写请求，未退回、批准或改元数据。成功写入/请求失败/pending 分支仅静态复核与服务器构建，未用真实申请试写；原点击未在有界 API 日志中匹配到请求，不能编造原返回码。
- 本轮入口修复已部署并推送：三个站内管理员入口、当前账号角色返回、管理页加载/登录/权限/重试状态；仅期刊页面和 API 改为 Nginx 内部实时会话/角色检查，管理 API 权限/CSRF/审计保持，ADR-003 已登记用户批准例外。独立 high GO，补齐 API 401 时明确登录入口；必要服务器构建、启动、Nginx 配置及精确版本切换成功，无迁移；日志 `.work/journal-admin-entry-deploy.log`。
- 真实 Edge 现有管理员会话：期刊目录顶部及申请按钮旁显示“期刊审核”，实际点击后显示指定真实申请、编号及通过/补件/拒绝按钮，截图已观察。匿名审核页 302 到固定登录返回地址、匿名管理 API 401、内部验证入口 404、其他管理接口仍 401；没有代审申请。未执行全套测试或用普通用户会话实测 403，后者仅静态守卫复核。
- 本轮英语规则与回执修订已推至 GitHub 并上线；服务器必要构建、组件健康、active CAS、公网精确版本核对成功，发布事务清除，无新迁移；日志 `.work/journal-english-receipt-deploy.log`。共享英文字符校验覆盖草稿、新提交、通过核验、主页修改和激活；人名支持拉丁重音，英文语义和官方名称仍需人工核验。
- 新修订增加本人申请/补件状态、真实编号和私有回执 `/journals/apply/{applicationId}`；提交成功跳转，响应丢失先只读恢复；已有申请可直接查看回执。英文优先显示，管理员从英文刊名生成并可编辑主页后缀。
- 新服务列表去除“专业审核”，逐项说明 AI 生成与材料整理的区别。旧草稿再次提交时由域层拒绝退役服务，已提交历史申请的审批和幂等保持。
- 独立 high 复核已 GO：修复学科输入空格被吞及旧草稿绕过退役服务的问题；未运行本地构建、测试、CI。修订时只读核实指定账号当时不是管理员、申请仍 submitted；权限后续变化见下条。
- 2026-09-16 用户明确授权后，指定机构邮箱的唯一已验证账号已由 user 改为 platform_admin；独立 high GO，单行身份/旧值条件更新与审计写入同一事务，提交后角色及审计读回成功。其余账号和申请未改，无代码、部署或测试；个人邮箱与审计详情仅保存在生产审计及私有执行记录。
- 真实 Edge 已查看用户指定旧申请：成功回执、真实编号、待审核状态和提交时间均可见；“我的申请”正确回到该记录，英文必填/中文选填及三项服务说明可见。匿名读取该申请 API 返回 401。仅用未保存表单定向观察中文英文刊名被前端拦截、学科 Quantum Optics 保留空格；未创建新记录或改变原申请。
- 旧申请含中文元数据，需管理员要求补充材料后由申请人改成英文；未自动翻译、退回或审批。实际新申请提交跳转仅经代码审查和服务器构建，未为验收提交虚构申请；真实模型/解析及全套验收本轮仍未执行。

### 前两轮上线记录（历史）

- 已完成静态整合：保留线上搜索/科研工作台/回收站/媒体能力；期刊使用独立公开 publicationNo 与冻结出版元数据，原文作者与平台解读作者分开。
- 新通用 POST 搜索、回收站及 presentation 参数入口纳入期刊边界；来源上传复用存储清理锁和 retained 状态。上述新增差异独立 high 静态审查 GO。
- deploy.sh 接受已验证的完整线上 rollback SHA；远端锁、active 精确匹配、不可变目录与镜像身份约束保持。采用当前生产 no-tests 流程，不恢复旧 CI/全套验收政策。
- 上次暂停前，版本保护捕获 ba184541 → bdf0d3fa，未切换服务。本次恢复后同源双库备份成功：core 44M、search 3.4M，7/7 轮；备份仅存服务器。
- 已修复 Windows 本机部署路径和 Git SSH 对含单引号用户目录的兼容；服务器必要构建及镜像构建成功，核心新增期刊迁移成功、Search 无待迁移项。10:33 canonical 切换完成、服务启动健康、Nginx 配置通过、active CAS 完成且事务清除，日志位于项目 `.work/journal-deploy-3617f154.log`。
- 原分支历史测试不等同此次跨生产版本整合已通过；本次仅必要服务器构建及已知阻塞的定向修复，不运行全套测试。
- 部署观察准备时发现 Nginx `/admin/` 将期刊管理页发往 API；新增 `/admin/journals` 精确 Web 路由并保留原 Basic Auth、API 角色门禁，独立 high 静态复审 GO。第二轮已成功上线此修复，使用 `--skip-migrate`；日志 `.work/journal-deploy-3cc5bedb.log`。
- 当时实读：active/rollback 精确匹配，实际 Nginx 文件与最终源一致，无部署事务、失败或保留待办标记；API/Worker/Parser/ScanSci/BGE 及数据服务健康，Web 运行。管理前端内部 HTTP 200；公网期刊目录/申请页 200、管理页/API 匿名 401、管理页尾斜杠 308。
- 真实 Edge 已打开期刊目录和入驻申请，页面布局可见；目录正确显示暂无公开期刊，申请表完整。公开目录 API/站点地图 200，未登录刊内接口 401。未创建假期刊、未提交申请或改动已有论文；未使用管理员身份完成业务操作验收。

## Done

- 免费申请与平台核验、独立期刊工作空间、ISSN 防重、主页、刊内角色及所有者移交后复核。
- DOI 预览/批量导入、无 DOI 录入、原始论文身份与个人 RO 隔离、逐篇来源/权利记录。
- 私有来源上传、扫描/解析链路、摘要或全文范围、AI 标准草稿、证据匹配、编辑对照与冲突保护。
- SQL 持久化队列、权限与租约复核、有界重试、试用/服务额度、预占/消耗/释放/过期账本。
- 审核指派、修订绑定、原子固定版本公开、原 DOI 引用优先、公开 API、限制和撤回。
- 平台服务报价/拒绝/取消、一次性开通、通知、基础统计和公开元数据白名单。
- 私密纠错工单、编辑处理与双向通知；公开站点地图按期刊分页，仅收录可访问版本。
- 通用 RO/工作空间/材料/Agent 等旧入口不得绕开期刊流程；审核员限于指派论文。
- 需求与商业方案入库，运行说明及独立 GitHub 期刊工作流齐备。

## 历史 Evidence（原 261a38f 分支，不覆盖新生产集成）

- 后端依赖、Domain、API、Worker 构建通过；既有编译后解析器检查 10/10。
- 完整默认回归：Domain 547、API 109 通过；默认跳过的专用数据库用例另行实际运行。
- 定向测试：Domain 20/20、API 16/16（含期刊及既有公开研究接口）、Worker 4/4、Web 5/5；真实 PostgreSQL 覆盖并发额度、上传容量、幂等、撤权、固定快照、私密纠错和公开数据隔离。
- `scripts/journals/verify-migration.mjs`：完整迁移 37、回退保留个人 RO/旧表、重应用 12 个表及 8 个 CHECK；独有临时库已清理。
- Web 正式构建及类型检查通过；文档 247 份 lint、docs-sync 8 项及结构检查通过。GitHub CI 构建顺序修复后已通过，最新分支状态以 PR Checks 为准。
- 2026-09-16 08:51–08:52（Asia/Shanghai）真实 Chromium 场景通过：6 个页面 × 桌面 1440 / 手机 375，无横向溢出；私密反馈提交/回复/登录门禁、服务申请、生成队列轮询下保留编辑、保存后提交审核、限制公开后地图移除及固定页 404 全部通过。
- 浏览器使用真实会话与 CSRF、正式作业预占/取消；隔离 API 端口 43141（本机 3001 拒绝绑定）。仅豁免既有匿名阅读偏好接口预期 401，其余浏览器错误仍作为失败。13 张成功截图位于忽略目录 `apps/web/test/visual/out/journals/`。

## Constraints / open risks

- 所有数据库及内容测试为本地合成材料；真实模型与生产 PDF/OCR sidecar 的端到端验收尚未执行，不声明已提高引用。
- 当前提供书目和平台交付/调用统计；未采集访客时返回 null，调用次数不代表 AI 引用。
- 个人对象关联/复制按 PRD 首期人工协助留存版本化授权，不自动迁移私有对象。支付、外部监测、Topic Hub、知识图谱、认证仍属后续产品范围。
- 功能已部署并默认启用；真实试点前按运行手册核实代表授权、来源许可及模型/解析实际质量。新表有业务数据时优先关闭功能/回退应用，不直接执行会删除期刊数据的 SQL rollback。
- `sources/` 与同步项目资料只读；密钥由部署程序使用，不进入对话、日志或 Git。

## Next action / read-first

1. 完成退回修改新增差异独立复核、canonical 发布与真实页面更正误拒；观察同编号/原审核意见保留及申请表可编辑，不代填资料、重提或批准，不新建虚构期刊。
2. 后续发布必须重新定锚 active，沿用 canonical no-tests 流程；不要部署旧 261a38f，也不要因文档收尾再次部署。
3. 选获准真实论文完成来源解析、AI 草稿、人工审核及固定版本发布；此项与引用效果仍未验收，不从服务健康推断科学质量。
4. 先读 [运行手册](../runbooks/journal-onboarding.md)、[部署手册](../runbooks/deployment.md) 与 [PRD](../specs/2026-09-15-journal-onboarding-design.md) 对应模块；Hermes handoff 属其他任务，保护其未完成目标，不自动续跑。
