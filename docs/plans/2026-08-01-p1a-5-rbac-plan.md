# P1A-5 RBAC 权限矩阵实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 P1A-4 散落的内联角色检查收敛为声明式「动作×角色」矩阵（packages/domain），并在 API 层加统一 preHandler 守卫，满足 Spec §3.3 / §17。

**Architecture:** 矩阵唯一事实源在 `packages/domain/src/workspace/permissions.ts`；API 守卫（`apps/api/src/routes/workspace-guard.ts`）与 domain 内 `requireAction` 双层共源执行；迁移 4 给 `User` 预留 `platformRole`（无消费方）。

**Tech Stack:** TypeScript（tsc strict）、Prisma 5.22、Fastify、zod、vitest、pnpm workspace。

**依据文档：** spec `docs/specs/2026-08-01-p1a-5-rbac-design.md`（design gate 四节已用户确认）。

## Global Constraints

- pnpm 一律 `npx pnpm@9.15.0 <cmd>`，不全局安装。
- 本机无 Docker：迁移**不**本机 deploy、集成测试**不**本机跑（云上执行，云上写操作前逐项用户确认）。
- 不读取/打印 `.env`。
- git mutation（add/commit/push）逐次需用户批准；计划中的「提交」步骤均为检查点，先问再做。
- 错误语义沿用 P1A-4：非成员/空间不存在 → 404 `WORKSPACE_NOT_FOUND`；角色不足 → 403 `FORBIDDEN`；未登录 → 401 `SESSION_INVALID`。
- 审计不接线（归 2.6），只在拒绝点留 `// audit(2.6): ...` 注释。
- task-master 2.5 按 test-gate 纪律保持 pending，云上集成测试全绿后才置 done。

---

### Task 1: 迁移 4 + schema（User.platformRole 预留）

**Files:**
- Create: `infra/migrations/20260801010000_user_platform_role/migration.sql`
- Create: `infra/migrations/20260801010000_user_platform_role/rollback.sql`
- Modify: `infra/schema.prisma`（User model 第 26–42 行区域 + 文件末尾 enum 区）

**Interfaces:**
- Consumes: 无（独立 schema 变更）。
- Produces: Prisma 生成类型 `PlatformRole`（`'user' | 'moderator' | 'platform_admin'`）与 `User.platformRole`；本计划后续任务**不消费**，仅供未来平台守卫使用。

- [ ] **Step 1: 写 migration.sql**

```sql
CREATE TYPE "PlatformRole" AS ENUM ('user', 'moderator', 'platform_admin');

ALTER TABLE "users" ADD COLUMN "platform_role" "PlatformRole" NOT NULL DEFAULT 'user';
```

- [ ] **Step 2: 写 rollback.sql**（沿用迁移 3 补偿风格）

```sql
-- Compensation rollback for 20260801010000_user_platform_role.
ALTER TABLE "users" DROP COLUMN IF EXISTS "platform_role";
DROP TYPE IF EXISTS "PlatformRole";
DELETE FROM "_prisma_migrations" WHERE "migration_name" = '20260801010000_user_platform_role';
```

- [ ] **Step 3: schema.prisma 加枚举与字段**

在 `enum WorkspaceRole {` 之前（或文件 enum 区任意位置）加：

```prisma
enum PlatformRole {
  user
  moderator
  platform_admin
}
```

在 `model User {` 的 `status UserStatus @default(invited)` 行之后加一行：

```prisma
  platformRole       PlatformRole       @default(user) @map("platform_role")
```

- [ ] **Step 4: 验证生成**

Run: `npx pnpm@9.15.0 --filter @openscience/database build`
Expected: exit 0（build 会先跑 `prisma generate`）；`node -e "const {PlatformRole}=require('./packages/database/node_modules/.prisma/client'); console.log(Object.values(PlatformRole))"` 输出含 `user, moderator, platform_admin`（路径不行就改查 `node_modules/.prisma/client/index.d.ts` 中 `PlatformRole` 字样：`grep -c "PlatformRole" node_modules/.prisma/client/index.d.ts` ≥ 1）。
注意：迁移**不** deploy（本机无 Docker，留云上）。

- [ ] **Step 5: 全仓 typecheck 回归**

Run: `npx pnpm@9.15.0 typecheck`
Expected: exit 0（schema 变更不破坏现有代码）。

- [ ] **Step 6: 提交检查点（需用户批准）**

```bash
git add infra/migrations/20260801010000_user_platform_role infra/schema.prisma
git commit -m "feat: P1A-5 迁移 4 User.platformRole 平台角色预留"
```

---

### Task 2: domain 权限矩阵（permissions.ts，TDD）

**Files:**
- Create: `packages/domain/src/workspace/permissions.ts`
- Create: `packages/domain/test/workspace-permissions.test.ts`
- Modify: `packages/domain/src/index.ts`（追加导出）

**Interfaces:**
- Consumes: `WorkspaceError`（`./errors`）、`Membership`/`WorkspaceRole`（`@prisma/client` 类型）。
- Produces（Task 3/4 依赖的确切签名）:
  - `WORKSPACE_ACTIONS: readonly ['workspace.read','workspace.update','workspace.archive','workspace.transfer','member.list','member.change_role','member.remove','invitation.create','invitation.revoke']`（as const）
  - `type WorkspaceAction = (typeof WORKSPACE_ACTIONS)[number]`
  - `ROLE_PERMISSIONS: Record<WorkspaceAction, readonly WorkspaceRole[]>`
  - `can(role: WorkspaceRole, action: WorkspaceAction): boolean`
  - `requireAction(membership: Pick<Membership, 'role'>, action: WorkspaceAction): void`（拒绝抛 `WorkspaceError('FORBIDDEN')`）

- [ ] **Step 1: 写失败测试**

`packages/domain/test/workspace-permissions.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import type { WorkspaceRole } from '@prisma/client';
import { WorkspaceError } from '../src/workspace/errors';
import { can, requireAction, ROLE_PERMISSIONS, WORKSPACE_ACTIONS } from '../src/workspace/permissions';

const ROLES: WorkspaceRole[] = ['owner', 'maintainer', 'author', 'contributor', 'reviewer', 'viewer'];

// 期望矩阵（刻意与实现各自手写，防同步改错）；C/D 类动作（leave/accept/decline/create/list-my）不经矩阵
const EXPECTED: Record<string, readonly WorkspaceRole[]> = {
  'workspace.read': ROLES,
  'workspace.update': ['owner', 'maintainer'],
  'workspace.archive': ['owner'],
  'workspace.transfer': ['owner'],
  'member.list': ROLES,
  'member.change_role': ['owner'],
  'member.remove': ['owner', 'maintainer'],
  'invitation.create': ['owner', 'maintainer'],
  'invitation.revoke': ['owner', 'maintainer'],
};

describe('RBAC 动作×角色矩阵', () => {
  it('动作清单恰为 9 个且与期望键集一致', () => {
    expect([...WORKSPACE_ACTIONS].sort()).toEqual(Object.keys(EXPECTED).sort());
  });

  it('6 角色 × 9 动作全笛卡尔积（54 条）与期望一致', () => {
    for (const role of ROLES) {
      for (const action of WORKSPACE_ACTIONS) {
        expect(can(role, action), `${role} × ${action}`).toBe(EXPECTED[action].includes(role));
      }
    }
  });

  it('ROLE_PERMISSIONS 覆盖每个动作（无遗漏键）', () => {
    for (const action of WORKSPACE_ACTIONS) expect(ROLE_PERMISSIONS[action]).toBeDefined();
  });

  it('requireAction：允许不抛；拒绝抛 WorkspaceError FORBIDDEN', () => {
    expect(() => requireAction({ role: 'owner' }, 'workspace.transfer')).not.toThrow();
    expect(() => requireAction({ role: 'viewer' }, 'workspace.update')).toThrowError(WorkspaceError);
    try {
      requireAction({ role: 'author' }, 'member.remove');
      expect.unreachable('应抛 FORBIDDEN');
    } catch (e) {
      expect((e as WorkspaceError).code).toBe('FORBIDDEN');
    }
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx pnpm@9.15.0 --filter @openscience/domain test`
Expected: FAIL（`../src/workspace/permissions` 不存在）。

- [ ] **Step 3: 写实现**

`packages/domain/src/workspace/permissions.ts`：

```ts
import type { Membership, WorkspaceRole } from '@prisma/client';
import { WorkspaceError } from './errors';

/**
 * P1A-5 RBAC：workspace 动作×角色矩阵（唯一事实源）。
 * API 守卫（apps/api workspace-guard）与 domain 内 requireAction 双层共源，均以本表为准。
 * 平台级角色（User.platformRole：moderator / platform_admin，迁移 4 预留）不参与本矩阵，
 * 未来平台守卫在 API 层单独判定；1B–1D 资源（RO/Branch/PR）各自导出矩阵片段复用 can() 机制。
 * C/D 类动作（member.leave / invitation.accept|decline / 认证即可端点）不经本矩阵，见 design spec §2。
 */

export const WORKSPACE_ACTIONS = [
  'workspace.read',
  'workspace.update',
  'workspace.archive',
  'workspace.transfer',
  'member.list',
  'member.change_role',
  'member.remove',
  'invitation.create',
  'invitation.revoke',
] as const;

export type WorkspaceAction = (typeof WORKSPACE_ACTIONS)[number];

const ALL_ROLES: readonly WorkspaceRole[] = ['owner', 'maintainer', 'author', 'contributor', 'reviewer', 'viewer'];

export const ROLE_PERMISSIONS: Record<WorkspaceAction, readonly WorkspaceRole[]> = {
  'workspace.read': ALL_ROLES,
  'workspace.update': ['owner', 'maintainer'],
  'workspace.archive': ['owner'],
  'workspace.transfer': ['owner'],
  'member.list': ALL_ROLES,
  'member.change_role': ['owner'],
  'member.remove': ['owner', 'maintainer'],
  'invitation.create': ['owner', 'maintainer'],
  'invitation.revoke': ['owner', 'maintainer'],
};

export function can(role: WorkspaceRole, action: WorkspaceAction): boolean {
  return ROLE_PERMISSIONS[action].includes(role);
}

/** domain 纵深判定：角色不足抛 FORBIDDEN（与既有 requireRole 语义一致）。 */
export function requireAction(membership: Pick<Membership, 'role'>, action: WorkspaceAction): void {
  if (!can(membership.role, action)) throw new WorkspaceError('FORBIDDEN', '权限不足');
}
```

- [ ] **Step 4: index.ts 追加导出**

在 `packages/domain/src/index.ts` 末尾追加：

```ts
export { can, requireAction, ROLE_PERMISSIONS, WORKSPACE_ACTIONS, type WorkspaceAction } from './workspace/permissions';
```

- [ ] **Step 5: 跑测试确认通过**

Run: `npx pnpm@9.15.0 --filter @openscience/domain test`
Expected: PASS（含新文件 4 用例；既有 32 用例不受影响）。

- [ ] **Step 6: 提交检查点（需用户批准）**

```bash
git add packages/domain/src/workspace/permissions.ts packages/domain/src/index.ts packages/domain/test/workspace-permissions.test.ts
git commit -m "feat: P1A-5 domain 动作×角色权限矩阵"
```

---

### Task 3: domain 7 处 requireRole → requireAction（共源收敛）

**Files:**
- Modify: `packages/domain/src/workspace/helpers.ts`（删除 `requireRole`）
- Modify: `packages/domain/src/workspace/workspaces.ts`（2 处）
- Modify: `packages/domain/src/workspace/members.ts`（3 处）
- Modify: `packages/domain/src/workspace/invitations.ts`（2 处）

**Interfaces:**
- Consumes: Task 2 的 `requireAction` / `WorkspaceAction`。
- Produces: domain 公共函数签名**不变**（纯内部替换），API 层无感知。

替换对照（行为与现状逐点一致）：

| 文件:行（现状） | 现状 | 替换为 |
|---|---|---|
| workspaces.ts:60 | `requireRole(membership, ['owner', 'maintainer'])` | `requireAction(membership, 'workspace.update')` |
| workspaces.ts:70 | `requireRole(membership, ['owner'])` | `requireAction(membership, 'workspace.archive')` |
| members.ts:35 | `requireRole(membership, ['owner'])` | `requireAction(membership, 'member.change_role')` |
| members.ts:59 | `requireRole(membership, ['owner', 'maintainer'])` | `requireAction(membership, 'member.remove')` |
| members.ts:94 | `requireRole(membership, ['owner'])` | `requireAction(membership, 'workspace.transfer')` |
| invitations.ts:34 | `requireRole(membership, ['owner', 'maintainer'])` | `requireAction(membership, 'invitation.create')` |
| invitations.ts:147 | `requireRole(membership, ['owner', 'maintainer'])` | `requireAction(membership, 'invitation.revoke')` |

- [ ] **Step 1: 改三个调用文件**

每个文件：import 从 `./helpers` 移除 `requireRole`、从 `./permissions` 加 `requireAction`；按上表逐处替换。`members.ts` 顶部 `import type { WorkspaceRole } from '@prisma/client'` 仍被 `newRole as WorkspaceRole` 使用，保留。

- [ ] **Step 2: helpers.ts 删除 requireRole**

删除：

```ts
export function requireRole(membership: Membership, roles: WorkspaceRole[]): void {
  if (!roles.includes(membership.role)) throw new WorkspaceError('FORBIDDEN', '权限不足');
}
```

若 `WorkspaceRole` import 因此不再使用，一并从 import 移除（`Membership` 仍被 `requireMembership`/`requireRole` 之外使用则保留——`requireMembership` 返回类型含 `Membership`，保留）。

- [ ] **Step 3: 确认零残留**

Run: `grep -rn "requireRole" packages/ apps/`
Expected: 无输出（0 处引用）。

- [ ] **Step 4: domain 全量单测回归**

Run: `npx pnpm@9.15.0 --filter @openscience/domain test`
Expected: PASS（36 用例：原 32 + 新 4；行为不变即共源证据）。

- [ ] **Step 5: typecheck**

Run: `npx pnpm@9.15.0 typecheck`
Expected: exit 0。

- [ ] **Step 6: 提交检查点（需用户批准）**

```bash
git add packages/domain/src/workspace
git commit -m "refactor: P1A-5 domain 角色检查收敛为矩阵 requireAction"
```

---

### Task 4: API 统一授权守卫（workspace-guard.ts，TDD）

**Files:**
- Create: `apps/api/src/routes/workspace-guard.ts`
- Create: `apps/api/test/workspace-guard.test.ts`
- Modify: `apps/api/src/routes/workspaces.ts`（9 条路由挂 preHandler）

**Interfaces:**
- Consumes: Task 2 的 `can` / `WorkspaceAction`（经 `@openscience/domain` 导出）；`AuthDeps`（`@openscience/auth`，含 `prisma`）；`requireCurrentUser`（`./session-guard`）。
- Produces: `requireWorkspaceAction(deps: AuthDeps, action: WorkspaceAction): preHandlerAsyncHookHandler`；`FastifyRequest.workspaceMembership?: Membership`（模块扩展，仅供日志/调试）。

- [ ] **Step 1: 写失败测试**

`apps/api/test/workspace-guard.test.ts`（fake 模式复用 workspace-routes.test.ts 的 setup 惯例）：

```ts
import { describe, expect, it } from 'vitest';
import { createSession } from '@openscience/auth';
import { buildApp } from '../src/app';
import { createFakeMailer, createFakePrisma, createFakeRedis } from './helpers/fakes';

/* eslint-disable @typescript-eslint/no-explicit-any -- 测试 fake 刻意脱离完整类型 */

const U1 = '11111111-1111-4111-8111-111111111111';
const U2 = '22222222-2222-4222-8222-222222222222';

async function setup() {
  const { prisma, db } = createFakePrisma();
  const redis = createFakeRedis();
  const mailer = createFakeMailer();
  const app = await buildApp({ prisma, redis, mailer, cookieSecret: 'test-secret', secureCookies: false });
  const loginAs = async (userId: string, email: string): Promise<string> => {
    db.users.push({ id: userId, email, displayName: 'User', passwordHash: 'x', status: 'email_verified', createdAt: new Date(), updatedAt: new Date() });
    return createSession(redis, { userId, status: 'email_verified' });
  };
  const authed = (token: string) => ({ cookies: { openscience_session: token } });
  const createTeam = async (token: string): Promise<string> => {
    const res = await app.inject({ method: 'POST', url: '/workspaces', ...authed(token), payload: { name: 'Lab' } });
    return res.json().id;
  };
  return { app, db, authed, loginAs, createTeam };
}

describe('P1A-5 workspace 授权守卫', () => {
  it('viewer 带非法 body PATCH → 403（守卫先于 body 校验）', async () => {
    const { app, db, authed, loginAs, createTeam } = await setup();
    const t1 = await loginAs(U1, 'a@example.com');
    const t2 = await loginAs(U2, 'b@example.com');
    const id = await createTeam(t1);
    db.memberships.push({ id: 'm-v', workspaceId: id, userId: U2, role: 'viewer', createdAt: new Date(), updatedAt: new Date() });
    const res = await app.inject({ method: 'PATCH', url: `/workspaces/${id}`, ...authed(t2), payload: {} });
    expect(res.statusCode).toBe(403);
    expect(res.json().error.code).toBe('FORBIDDEN');
  });

  it('非成员 PATCH → 404 WORKSPACE_NOT_FOUND', async () => {
    const { app, authed, loginAs, createTeam } = await setup();
    const t1 = await loginAs(U1, 'a@example.com');
    const t2 = await loginAs(U2, 'b@example.com');
    const id = await createTeam(t1);
    const res = await app.inject({ method: 'PATCH', url: `/workspaces/${id}`, ...authed(t2), payload: { name: 'X' } });
    expect(res.statusCode).toBe(404);
    expect(res.json().error.code).toBe('WORKSPACE_NOT_FOUND');
  });

  it('无 session PATCH → 401 SESSION_INVALID', async () => {
    const { app, loginAs, createTeam } = await setup();
    const t1 = await loginAs(U1, 'a@example.com');
    const id = await createTeam(t1);
    const res = await app.inject({ method: 'PATCH', url: `/workspaces/${id}`, payload: { name: 'X' } });
    expect(res.statusCode).toBe(401);
    expect(res.json().error.code).toBe('SESSION_INVALID');
  });

  it('非法 uuid 路径参数 → 400（zod 在守卫内先行校验，不落到 prisma）', async () => {
    const { app, authed, loginAs } = await setup();
    const t1 = await loginAs(U1, 'a@example.com');
    const res = await app.inject({ method: 'PATCH', url: '/workspaces/not-a-uuid', ...authed(t1), payload: { name: 'X' } });
    expect(res.statusCode).toBe(400);
  });

  it('maintainer PATCH → 200（守卫与 domain 双层放行）', async () => {
    const { app, db, authed, loginAs, createTeam } = await setup();
    const t1 = await loginAs(U1, 'a@example.com');
    const t2 = await loginAs(U2, 'b@example.com');
    const id = await createTeam(t1);
    db.memberships.push({ id: 'm-m', workspaceId: id, userId: U2, role: 'maintainer', createdAt: new Date(), updatedAt: new Date() });
    const res = await app.inject({ method: 'PATCH', url: `/workspaces/${id}`, ...authed(t2), payload: { name: 'Renamed' } });
    expect(res.statusCode).toBe(200);
    expect(res.json().name).toBe('Renamed');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx pnpm@9.15.0 --filter @openscience/api test`
Expected: FAIL（第 1 条非法 body 用例现状返回 400 而非 403；守卫尚不存在）。

- [ ] **Step 3: 写守卫实现**

`apps/api/src/routes/workspace-guard.ts`：

```ts
import type { FastifyReply, FastifyRequest, preHandlerAsyncHookHandler } from 'fastify';
import type { Membership } from '@prisma/client';
import { z } from 'zod';
import type { AuthDeps } from '@openscience/auth';
import { can, type WorkspaceAction } from '@openscience/domain';
import { requireCurrentUser } from './session-guard';

const NOT_FOUND_BODY = { error: { code: 'WORKSPACE_NOT_FOUND', message: '空间不存在' } } as const;
const FORBIDDEN_BODY = { error: { code: 'FORBIDDEN', message: '权限不足' } } as const;
const idParam = z.object({ id: z.string().uuid() });

declare module 'fastify' {
  interface FastifyRequest {
    /** P1A-5 守卫解析出的成员身份，仅供日志/调试；domain 仍自行重查（双层共源，签名不变）。 */
    workspaceMembership?: Membership;
  }
}

/**
 * P1A-5 API 授权边界（Spec §3.3/§17）：session → workspace 归属 → 角色矩阵。
 * 空间不存在或非成员 → 404（不泄露存在性）；成员但角色不足 → 403；未登录 → 401。
 */
export function requireWorkspaceAction(deps: AuthDeps, action: WorkspaceAction): preHandlerAsyncHookHandler {
  return async (req: FastifyRequest, reply: FastifyReply) => {
    const user = await requireCurrentUser(deps, req, reply);
    if (!user) return; // 401 已由 session-guard 发送
    const { id } = idParam.parse(req.params);
    const workspace = await deps.prisma.workspace.findUnique({ where: { id } });
    const membership = workspace
      ? await deps.prisma.membership.findUnique({
          where: { workspaceId_userId: { workspaceId: id, userId: user.userId } },
        })
      : null;
    if (!workspace || !membership) {
      // audit(2.6): authz.deny（workspace 不存在或非成员）
      return reply.status(404).send(NOT_FOUND_BODY);
    }
    if (!can(membership.role, action)) {
      // audit(2.6): authz.deny（角色不足）
      return reply.status(403).send(FORBIDDEN_BODY);
    }
    req.workspaceMembership = membership;
  };
}
```

- [ ] **Step 4: workspaces.ts 挂接 9 条路由**

import 追加：

```ts
import { requireWorkspaceAction } from './workspace-guard';
```

逐条加 preHandler（handler 体完全不动，C/D 类不挂）：

```ts
app.get('/:id', { preHandler: requireWorkspaceAction(deps, 'workspace.read') }, async (req, reply) => { ... });
app.patch('/:id', { preHandler: requireWorkspaceAction(deps, 'workspace.update') }, async (req, reply) => { ... });
app.post('/:id/archive', { preHandler: requireWorkspaceAction(deps, 'workspace.archive') }, async (req, reply) => { ... });
app.get('/:id/members', { preHandler: requireWorkspaceAction(deps, 'member.list') }, async (req, reply) => { ... });
app.post('/:id/invitations', { preHandler: requireWorkspaceAction(deps, 'invitation.create') }, async (req, reply) => { ... });
app.delete('/:id/invitations/:invId', { preHandler: requireWorkspaceAction(deps, 'invitation.revoke') }, async (req, reply) => { ... });
app.patch('/:id/members/:userId', { preHandler: requireWorkspaceAction(deps, 'member.change_role') }, async (req, reply) => { ... });
app.delete('/:id/members/:userId', { preHandler: requireWorkspaceAction(deps, 'member.remove') }, async (req, reply) => { ... });
app.post('/:id/transfer', { preHandler: requireWorkspaceAction(deps, 'workspace.transfer') }, async (req, reply) => { ... });
```

不挂守卫（维持现状）：`GET /`、`POST /`、`GET /invitations`、`POST /invitations/:id/accept`、`POST /invitations/:id/decline`、`POST /:id/leave`。

- [ ] **Step 5: 跑测试确认通过 + api 全量回归**

Run: `npx pnpm@9.15.0 --filter @openscience/api test`
Expected: PASS（原 29 + 新 5 = 34 用例；既有「viewer 改资料 → 403」「非成员详情 → 404」等用例现由守卫层先行拦截，状态码/错误码不变）。

- [ ] **Step 6: typecheck + lint**

Run: `npx pnpm@9.15.0 typecheck && npx pnpm@9.15.0 lint`
Expected: 均 exit 0（含 `WORKSPACE_STRUCTURE_OK`）。

- [ ] **Step 7: 提交检查点（需用户批准）**

```bash
git add apps/api/src/routes/workspace-guard.ts apps/api/src/routes/workspaces.ts apps/api/test/workspace-guard.test.ts
git commit -m "feat: P1A-5 API 统一 workspace 授权守卫（preHandler）"
```

---

### Task 5: 云上集成用例 + 全门禁 + task-master 修订 + docs-sync

**Files:**
- Modify: `apps/api/test/workspaces.integration.test.ts`（追加 describe，本机写码、云上执行）
- Modify: task-master 2.5 details（MCP `update-subtask` 或直接改 tasks.json，落点 auth → domain 的已确认偏离）
- Modify: `docs/progress.md`（新条目置顶）、`project_index.md`（登记 spec/plan）

**Interfaces:**
- Consumes: Task 1–4 全部产物。
- Produces: task-master 2.5 仍 pending（云上全绿后才置 done）。

- [ ] **Step 1: 集成测试追加 describe**（追加到 `workspaces.integration.test.ts` 末尾，复用文件内 `makeApp` / `registerAndVerify`）

```ts
describe('P1A-5 RBAC 守卫（云上）', () => {
  it('viewer 成员 PATCH → 403；非法 body 也先 403（守卫先于 body 校验）', async () => {
    const app = await makeApp();
    const ownerCookie = await registerAndVerify(app, 'rbac-owner@example.com');
    const team = await app.inject({
      method: 'POST',
      url: '/workspaces',
      cookies: { openscience_session: ownerCookie },
      payload: { name: 'RBAC Lab' },
    });
    const teamId = team.json().id;
    const invited = await app.inject({
      method: 'POST',
      url: `/workspaces/${teamId}/invitations`,
      cookies: { openscience_session: ownerCookie },
      payload: { email: 'rbac-viewer@example.com', role: 'viewer' },
    });
    const viewerCookie = await registerAndVerify(app, 'rbac-viewer@example.com');
    const accepted = await app.inject({
      method: 'POST',
      url: `/workspaces/invitations/${invited.json().invitationId}/accept`,
      cookies: { openscience_session: viewerCookie },
    });
    expect(accepted.statusCode).toBe(201);

    const denied = await app.inject({
      method: 'PATCH',
      url: `/workspaces/${teamId}`,
      cookies: { openscience_session: viewerCookie },
      payload: { name: 'x' },
    });
    expect(denied.statusCode).toBe(403);
    expect(denied.json().error.code).toBe('FORBIDDEN');

    const deniedBadBody = await app.inject({
      method: 'PATCH',
      url: `/workspaces/${teamId}`,
      cookies: { openscience_session: viewerCookie },
      payload: {},
    });
    expect(deniedBadBody.statusCode).toBe(403);
    await app.close();
  });

  it('非成员 PATCH → 404；无 session PATCH → 401', async () => {
    const app = await makeApp();
    const ownerCookie = await registerAndVerify(app, 'rbac-owner2@example.com');
    const team = await app.inject({
      method: 'POST',
      url: '/workspaces',
      cookies: { openscience_session: ownerCookie },
      payload: { name: 'RBAC Lab 2' },
    });
    const outsiderCookie = await registerAndVerify(app, 'rbac-outsider@example.com');
    const res404 = await app.inject({
      method: 'PATCH',
      url: `/workspaces/${team.json().id}`,
      cookies: { openscience_session: outsiderCookie },
      payload: { name: 'x' },
    });
    expect(res404.statusCode).toBe(404);
    expect(res404.json().error.code).toBe('WORKSPACE_NOT_FOUND');
    const res401 = await app.inject({ method: 'PATCH', url: `/workspaces/${team.json().id}`, payload: { name: 'x' } });
    expect(res401.statusCode).toBe(401);
    await app.close();
  });
});
```

- [ ] **Step 2: 本机验证收集与类型（不运行）**

Run: `npx pnpm@9.15.0 --filter @openscience/api exec vitest run --config vitest.integration.config.ts --collect`（或该包既有 collect 方式）
Expected: 新 2 用例被列出；`npx pnpm@9.15.0 typecheck` exit 0。

- [ ] **Step 3: 本机全门禁**

Run: `npx pnpm@9.15.0 build && npx pnpm@9.15.0 test && npx pnpm@9.15.0 lint && npx pnpm@9.15.0 audit:knip && npx pnpm@9.15.0 audit:dep && npx pnpm@9.15.0 audit:deps && npx pnpm@9.15.0 audit:dup && npx pnpm@9.15.0 docs:lint`
Expected: 全部 exit 0；单测总数 = 107 + 4（domain 矩阵）+ 5（api 守卫）= 116。

- [ ] **Step 4: task-master 2.5 details 修订**

把 details 中「packages/auth 提供权限判定与 API 中间件/守卫」修订为「权限矩阵与 requireAction 在 packages/domain（workspace 模块），API preHandler 守卫在 apps/api；auth 包保持纯身份层」（design gate 用户已确认的偏离）。验收补「矩阵 54 断言 + 守卫 5 单测 + 云上 2 集成用例」。

- [ ] **Step 5: docs-sync**

- `docs/progress.md` 新条目置顶：P1A-5 design gate 通过 + 实现完成（本机门禁证据）+ 云上集成待执行。
- `project_index.md`：spec/plan 两文件状态更新为「已批准/已实现（云上集成待执行）」。

- [ ] **Step 6: 提交检查点（需用户批准）**

```bash
git add apps/api/test/workspaces.integration.test.ts docs/progress.md project_index.md docs/specs/2026-08-01-p1a-5-rbac-design.md docs/plans/2026-08-01-p1a-5-rbac-plan.md
git commit -m "test+docs: P1A-5 RBAC 集成用例与文档登记"
```

- [ ] **Step 7: 云上收口（云上写操作前逐项用户确认）**

代码 tar-over-ssh 同步至 `/opt/openscience` 后：

```bash
cd /opt/openscience
npx pnpm@9.15.0 install && npx pnpm@9.15.0 --filter @openscience/database build   # 先让 prisma generate 跑（已知坑）
node packages/database/dist/migrate-cli.js deploy                                 # 迁移 4 落库
npx pnpm@9.15.0 test:integration
```

Expected: migrate deploy 含 `20260801010000_user_platform_role` applied；集成测试 11/11（原 9 + 新 2）全绿。全绿后置 task-master 2.5 done，并更新 progress.md。

---

## Self-Review 记录

- Spec 覆盖：§1 架构 → Task 2/3/4；§2 矩阵 → Task 2；§3 迁移+守卫 → Task 1/4；§4 测试与验收 → Task 2/4/5。无缺口。
- 占位符：无 TBD/TODO；所有代码步骤含完整代码。
- 类型一致性：`can`/`requireAction`/`WorkspaceAction`/`requireWorkspaceAction`/`WORKSPACE_ACTIONS`/`ROLE_PERMISSIONS` 在 Task 2 定义、Task 3/4/5 引用，拼写一致；`EXPECTED` 键集与 `WORKSPACE_ACTIONS` 一致（9 个）。
- 风险点：Task 4 Step 4 挂接后既有用例「非法请求体 → 400」是 owner 调用（守卫放行后 handler zod 400），不受守卫影响；「personal/归档 → 409」同理（owner 过守卫，domain 不变量抛 409）。
