# P1C-6 Pull Request 声明与提交流程 — 实施计划

- 日期：2026-08-04
- 任务：task-master 4.6
- 依据：`docs/specs/2026-08-04-p1c-6-pr-design.md`（5 决策已确认）

---

## 五决策（已确认）

| Q | 决策 |
|---|------|
| Q1 | §8.2 声明 domain 强制校验（全字段必填 + CRediT/license 校验） |
| Q2 | PR 详情含分支 diff（compareVersions 复用） |
| Q3 | 创建时许可继承校验（源 text + PR code/data），违反 409 |
| Q4 | pull_request.opened 落 Notification 行（P1D-2 幂等重放） |
| Q5 | 迁移 14：pull_requests.idempotency_key @unique |

## TDD 步骤

1. **迁移 14**：`20260804060000_pr_idempotency/` — ADD COLUMN idempotency_key UUID? + UNIQUE + rollback
   - 实际：`ALTER TABLE pull_requests ADD COLUMN idempotency_key TEXT; CREATE UNIQUE INDEX ... ON pull_requests(idempotency_key)`（null 可多行，唯一不冲突）
2. **schema.prisma**：PullRequest + idempotencyKey String? @unique
3. **domain `pr/errors.ts`**：PrError + 错误码（RESEARCH_OBJECT_NOT_FOUND/FORBIDDEN/VALIDATION_ERROR/INHERITANCE_VIOLATION/CROSS_RO_BRANCH）
4. **domain `pr/prs.ts`**：
   - `createPullRequest(deps, input, ctx)`：
     - requireMembership + 源/目标分支同 RO（CROSS_RO_BRANCH）
     - §8.2 强制校验：changedSdfFields/changedFiles 非空数组、newContributors 含 CRediT、dataLicense/codeLicense 目录校验、conflictOfInterest 非空、autoChecks 对象、布尔字段
     - 幂等键重放：idempotencyKey 已存在 → 返回既有 PR
     - 许可继承校验（Q3）：源 RO 有效许可 → validateLicenseInheritance(源, {text: 源.text, code: codeLicense, data: dataLicense})
     - 创建 PR + Notification（pull_request.opened）+ 审计
   - `listPullRequests(deps, {researchObjectId, userId?, status?})`：canAccessRo + _count.comments
   - `getPullRequest(deps, {researchObjectId, userId?, prId})`：canAccessRo + 详情 + diff（compareVersions target tip → source tip 版本）
5. **单测**：§8.2 校验/幂等/许可继承/跨 RO/越权
6. **domain index** 导出
7. **API `routes/prs.ts`**：POST/GET /research-objects/:id/pull-requests + GET /:prId + RATE_LIMIT_ROUTES 加行
8. **fake prisma**：pullRequest create/findUnique/findMany/findFirst + notification
9. **集成测试**：PR 全流程 + 幂等 + diff + 越权
10. **本地门禁**
11. **云上集成测试**
12. **文档同步** + task-master 4.6 done

## 验收对照

- §8.2：全声明字段提交时强制校验 ✅
- §4.2：PR 继承 RO 可见性 ✅
- §7.3：分支 diff 复用 ✅
- §16：幂等键 + pull_request.opened 事件（Notification 投递）✅
- §17：写审计 + 越权防护 ✅
- §21.2 步骤 11：提交 PR ✅
- 既有 74/74 不回退

## 风险

- diff 需两分支 tip 版本：Commit.branchId → version by commitId 链
- 迁移 14 UNIQUE index on nullable column：Postgres 允许多 null
- Notification 清理：collab afterAll 已有 notification.deleteMany
