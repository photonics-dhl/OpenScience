{
  "name": "@openscience/ai-gateway",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "selftest:ocr-contract": "node dist/ocr-contract-self-test.js"
  },
  "dependencies": {
    "@openscience/observability": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "vitest": "^2"
  }
}
